{
  "name": "mas",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:client": "turbo run dev --filter=mas-frontend",
    "dev:server": "turbo run dev --filter=mas-backend"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "puppeteer"]
  }
}
